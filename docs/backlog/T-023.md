# T-023: 참여자 탈퇴 처리(본인 계정만 삭제, 등록 데이터는 유지) API

> 문서 기준 backlog 스냅샷: sourceHash a816a52d678ed6d3…, 이 태스크의 현재 status: **todo**

## 목적

개발요청서.md의 A. 계정 및 협업 모델 "탈퇴 시 데이터: 참여자가 탈퇴해도 본인이 등록한 후보/코멘트는 삭제되지 않고 공간에 그대로 남음"에 따라, 사용자가 탈퇴할 때 계정은 삭제되지만 등록한 데이터는 유지되도록 처리합니다.

## 쉬운 설명

사용자가 탈퇴하면 auth.users 계정은 삭제되지만, 이전에 등록한 후보, 코멘트 등의 데이터는 남아 있습니다.

## 필요한 입력

- T-006 (couples 테이블)
- T-017 (프로필 관리)

## 결과물

- `deleteAccount()` 서버 액션
- auth.users 삭제 (Supabase Auth API 호출)
- candidates, comments 등의 created_by는 NULL로 설정 (또는 데이터 유지)

## 선행 작업

- T-006, T-017

## 수행 순서 (권장)

1. deleteAccount 서버 액션 구현
2. Supabase Admin API로 auth.users 계정 삭제 (require service role key)
3. couples 또는 couple_members에서 사용자 제거

## done_when 확인 방법

- 탈퇴 후 계정이 삭제되는가?
- 등록한 데이터가 유지되는가?

## 확인 질문

- created_by NULL 처리의 정확한 동작(관리자만 보이는지, 완전히 숨겨지는지 등)이 명확하지 않습니다.
