# T-006: Supabase DB 스키마: couple(커플 연결) 테이블 생성

> 문서 기준 backlog 스냅샷: sourceHash a816a52d678ed6d3…, 이 태스크의 현재 status: **done**

## 목적

개발요청서.md의 A. 계정 및 협업 모델 "각자 개별 계정으로 가입 후 '커플 연결'"에 따라, 두 사용자를 연결하고 공유된 데이터에 접근할 수 있게 하는 `couples` 및 `couple_members` 테이블을 생성합니다.

## 쉬운 설명

커플(두 사람)을 나타내는 `couples` 테이블과, 커플에 속한 멤버를 관리하는 `couple_members` 테이블로, "누가 이 결혼 준비를 함께 진행하는가"를 기록합니다.

## 필요한 입력

- T-003 (Supabase 프로젝트)
- Supabase 마이그레이션 파일

## 결과물

- `couples` 테이블 (id, created_at, wedding_date 등)
- `couple_members` 테이블 (couple_id, user_id, role, status, created_at 등)
- RLS 정책 (승인된 멤버만 접근 가능)

## 선행 작업

- T-003 (Supabase 프로젝트)

## 수행 순서 (권장)

1. `npx supabase migration new create_couples_table` 실행
2. 마이그레이션에 couples/couple_members 테이블 정의
3. 초대 코드(invitation_code), 상태(invited/accepted/rejected) 필드 포함
4. RLS 정책: couple_members 멤버만 couples 조회 가능
5. `npx supabase db push` 적용

## done_when 확인 방법

- `couples`, `couple_members` 테이블이 생성되었는가?
- RLS가 정상 작동하는가 (anon 키로 select 시 0 rows)?
- 현재 상태: "couples/couple_members 테이블+RLS 마이그레이션 적용 완료"

## 확인 질문

없음 — 완료 상태가 명확합니다.
