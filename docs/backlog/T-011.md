# T-011: Supabase DB 스키마: wedding_date(D-day) 필드 추가

> 문서 기준 backlog 스냅샷: sourceHash a816a52d678ed6d3…, 이 태스크의 현재 status: **done**

## 목적

개발요청서.md의 G. 기능 범위 "MVP 기능 범위: D-day 카운트다운"에 따라, 결혼식 날짜를 저장하는 `wedding_date` 필드를 `couples` 테이블에 추가합니다.

## 쉬운 설명

커플이 설정한 결혼식 날짜를 저장하여, 대시보드에서 결혼식까지 남은 일수를 표시할 수 있게 합니다.

## 필요한 입력

- T-006 (couples 테이블이 이미 생성되었을 것)

## 결과물

- `couples` 테이블에 `wedding_date` 필드 추가 (date 타입, nullable)

## 선행 작업

- T-006

## 수행 순서 (권장)

1. `npx supabase migration new add_wedding_date_to_couples`
2. `ALTER TABLE couples ADD COLUMN wedding_date DATE` 추가
3. `npx supabase db push`

## done_when 확인 방법

- `couples.wedding_date` 필드가 존재하는가?
- 현재 상태: "couples.wedding_date 필드 마이그레이션 적용 완료"

## 확인 질문

없음.
