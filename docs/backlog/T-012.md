# T-012: Supabase RLS 정책: 초대·승인된 참여자만 접근 가능하도록 설정

> 문서 기준 backlog 스냅샷: sourceHash a816a52d678ed6d3…, 이 태스크의 현재 status: **done**

## 목적

개발요청서.md의 F. 데이터/보안/운영 "기본 비공개 — 초대되어 승인된 참여자만 접근 가능"에 따라, 모든 테이블에 Row-Level Security(RLS) 정책을 적용하여 권한 없는 사용자의 접근을 차단합니다.

## 쉬운 설명

초대받지 않은 사용자나 로그인하지 않은 사용자는 어떤 데이터도 볼 수 없도록, Supabase RLS 정책을 통합적으로 설정합니다.

## 필요한 입력

- T-006, T-007, T-008, T-009, T-010 (모든 테이블이 생성되었을 것)

## 결과물

- RLS 정책 완성:
  - `workspaces`: couple_members만 접근
  - `candidates`: workspace 소유 couple의 멤버만 접근
  - `couples`: couple_members 목록 확인
  - `couple_members`: 자신의 초대 및 수락 상태만 조회
  - `checklist_items`, `budget_items`: couple 멤버만 접근
  - `profiles`: 자신의 프로필만 조회/수정
- `is_couple_member()` security definer 함수 (RLS 재귀 버그 수정)

## 선행 작업

- T-006, T-007, T-008, T-009, T-010

## 수행 순서 (권장)

1. Supabase 대시보드에서 각 테이블별 RLS 정책 작성
2. `is_couple_member(couple_id)` security definer 함수 생성
3. 각 테이블 정책에서 이 함수 활용
4. anon 키로 테스트 (접근 차단되어야 함, status 200 with 0 rows)

## done_when 확인 방법

- anon 키로 각 테이블(workspaces, candidates, couples, couple_members, checklist_items, budget_items, profiles) select 시 0 rows 반환되는가?
- 현재 상태: "비인증 사용자(anon 키)로 7개 테이블 전체 조회 시 0 rows 확인 — 비공개 기본 정책 정상 동작"

## 확인 질문

- 인증된 멤버의 실제 데이터 접근 가능성(positive path)은 로그인 UI(T-016) 완성 후 재검증 필요

