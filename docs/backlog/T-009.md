# T-009: Supabase DB 스키마: checklist 테이블 생성

> 문서 기준 backlog 스냅샷: sourceHash a816a52d678ed6d3…, 이 태스크의 현재 status: **done**

## 목적

개발요청서.md의 G. 기능 범위 "MVP 기능 범위: 결혼 준비 체크리스트"에 따라, 결혼 준비 과정 중 할 일을 체크리스트로 관리하는 `checklist_items` 테이블을 생성합니다.

## 쉬운 설명

커플이 함께 관리하는 결혼 준비 체크리스트 항목(예: "청첩장 인쇄", "웨딩 케이크 주문" 등)을 저장하고, 완료 여부를 토글할 수 있습니다.

## 필요한 입력

- T-006 (couples 테이블 — 커플 단위 데이터)
- Supabase 마이그레이션 파일

## 결과물

- `checklist_items` 테이블:
  - `id`, `couple_id`
  - `title` (체크리스트 항목)
  - `is_completed` (boolean, 기본값: false)
  - `created_at`, `updated_at`
- RLS 정책 (couple 멤버만 접근)

## 선행 작업

- T-006 (couples 테이블)

## 수행 순서 (권장)

1. `npx supabase migration new create_checklist_items_table`
2. couple_id 참조, title, is_completed 필드 정의
3. RLS 정책
4. `npx supabase db push`

## done_when 확인 방법

- `checklist_items` 테이블이 생성되었는가?
- RLS가 정상 작동하는가?
- 현재 상태: "checklist_items 테이블+RLS 마이그레이션 적용 완료"

## 확인 질문

없음.
