# T-008: Supabase DB 스키마: candidate(후보) 테이블 생성

> 문서 기준 backlog 스냅샷: sourceHash a816a52d678ed6d3…, 이 태스크의 현재 status: **done**

## 목적

개발요청서.md의 B. 데이터 모델 "후보 등록 필드: 링크, 위치, 코멘트 + 가격, 사진, 예약 상태·날짜, 평점 — 전부 선택 입력"에 따라, 각 항목(workspace) 내의 후보 장소를 저장하는 `candidates` 테이블을 생성합니다.

## 쉬운 설명

워크플레이스 내에서 후보로 등록된 각 장소(예: 을지로 스튜디오, 강남역 메이크업샵)의 정보(링크, 위치, 코멘트, 가격, 사진, 예약, 평점, **상태**)를 저장합니다.

## 필요한 입력

- T-003 (Supabase 프로젝트)
- T-007 (workspaces 테이블)

## 결과물

- `candidates` 테이블:
  - `id`, `workspace_id`, `created_by`(user_id)
  - `link`, `location` (선택), `comment` (코멘트)
  - `price_range`, `photo_url`, `reservation_status`, `reservation_date`, `rating` (선택)
  - `status` enum: `candidate` / `pending` / `rejected` / `confirmed` (필수, 개발요청서 B 요구사항)
  - `created_at`, `updated_at`
- RLS 정책 (workspace의 couple 멤버만 접근)

## 선행 작업

- T-003, T-007

## 수행 순서 (권장)

1. `npx supabase migration new create_candidates_table`
2. status enum 정의 (candidate/pending/rejected/confirmed 4종)
3. created_by는 `on delete set null` (탈퇴 후에도 데이터 보존)
4. RLS 정책
5. `npx supabase db push`

## done_when 확인 방법

- `candidates` 테이블이 생성되었는가?
- status 필드가 4가지 enum 값으로 제한되는가?
- 현재 상태: "candidates 테이블(상태 enum 4종 포함)+RLS 마이그레이션 적용 완료"

## 확인 질문

없음.
