# T-049: Supabase RLS 정책 최종 점검 및 테스트(비공개 접근 검증)

> 문서 기준 backlog 스냅샷: sourceHash a816a52d678ed6d3…, 이 태스크의 현재 status: **todo**

## 목적

개발요청서.md의 F. 데이터/보안/운영 "기본 비공개 — 초대되어 승인된 참여자만 접근 가능"이 정말로 구현되었는지 최종 확인합니다.

## 쉬운 설명

모든 기능(초대 코드, 소셜 로그인, 참여자 관리 등)이 완성된 후, 실제 권한 없는 사람이 데이터에 접근할 수 없는지, 권한이 있는 사람은 접근할 수 있는지를 테스트합니다.

## 필요한 입력

- T-012 (RLS 정책 기본 설정)
- T-018 (커플 연결 초대)
- T-020 (참여자 초대)

## 결과물

- RLS 정책 최종 검증 보고서
- 보안 침투 테스트 결과

## 선행 작업

- T-012, T-018, T-020

## 수행 순서 (권장)

1. 로그인하지 않은 상태에서 데이터 조회 시도 (모두 거부되어야 함)
2. 커플 연결 전 상태에서 조회 시도 (거부되어야 함)
3. 커플 연결 후 조회 시도 (성공해야 함)
4. 초대받지 않은 참여자로 조회 시도 (거부되어야 함)

## done_when 확인 방법

- 모든 음수 경로(unauthorized access)가 차단되는가?
- 모든 양수 경로(authorized access)가 작동하는가?

## 확인 질문

현재 status: **todo** — 로그인 UI(T-016~)가 미구현 상태이므로, 이 태스크는 모든 auth 기능 완료 후 진행 가능
