# T-001: Next.js(TypeScript, App Router) 프로젝트 초기화

> 문서 기준 backlog 스냅샷: sourceHash a816a52d678ed6d3… (E2), 이 태스크의 현재 status: **done**
> (이 문서가 존재한다고 해서 이 태스크의 실제 개발이 끝난 것은 아닙니다.)

## 목적

개발요청서.md의 E. 플랫폼 섹션 "기술 스택: 특정 프레임워크/클라우드 지정 없음 — 개발자 재량"에 따라, Next.js(TypeScript, App Router)를 선택하고 프로젝트를 초기화합니다. 이것이 전체 애플리케이션의 출발점입니다.

## 쉬운 설명

Next.js를 사용하여 새로운 웹 프로젝트를 만들고, 기본 구조를 설정합니다. TypeScript를 켜서 코드 품질을 높이고, App Router(Next.js 최신 방식)를 사용해 페이지를 관리합니다.

## 필요한 입력

- Node.js 설치 완료
- npm 또는 yarn 같은 패키지 매니저
- CLAUDE.md에 정의된 프로젝트 규칙 이해

## 결과물

- 실행 가능한 Next.js 프로젝트 구조 (app/, components/, lib/ 등)
- `package.json` (Next.js 16, TypeScript 설정 포함)
- `tsconfig.json` (TypeScript 설정)
- `.env.local` 템플릿 (환경변수 준비)
- git repository 초기화

## 선행 작업

선행 조건 없음. 프로젝트의 가장 첫 번째 작업입니다.

## 수행 순서 (권장)

1. `npx create-next-app@latest` 또는 `next create-app` 실행
2. TypeScript, Tailwind CSS, ESLint 옵션 선택
3. App Router 활성화 확인
4. 프로젝트 루트를 git repository로 초기화
5. `.claude/` 디렉터리(backlog 관리 도구) 복사

## done_when 확인 방법

- `npm run build`가 성공하고 정적 페이지 생성되는가?
- `npm run dev`로 서버가 정상 실행되는가?
- TypeScript 컴파일이 오류 없이 통과하는가?
- 기본 페이지(예: `/`)가 브라우저에서 표시되는가?
- 현재 상태: "Next.js 16(App Router,TS)+Tailwind v4 스캐폴드 생성, npm run build 성공(정적 페이지 2개 생성 확인)"

## 확인 질문

없음 — 현재 상태가 명확히 기록되어 있습니다.
