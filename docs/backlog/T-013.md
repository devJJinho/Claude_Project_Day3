# T-013: Supabase Auth 구글 소셜로그인 연동

> 문서 기준 backlog 스냅샷: sourceHash a816a52d678ed6d3…, 이 태스크의 현재 status: **needs_info**

## 목적

개발요청서.md의 A. 계정 및 협업 모델 "로그인 방식: 소셜 로그인(카카오/구글/네이버 등)"에 따라, Google OAuth를 통한 로그인을 Supabase와 연동합니다.

## 쉬운 설명

사용자가 Google 계정으로 로그인할 수 있도록 설정합니다.

## 필요한 입력

- T-003 (Supabase 프로젝트 생성)
- Google Cloud Console 접근 권한
- OAuth 2.0 클라이언트(웹 앱) 생성

## 결과물

- Google OAuth 클라이언트 ID/시크릿 (Google Cloud Console에서)
- Supabase Dashboard > Authentication > Providers > Google에서 활성화

## 선행 작업

- T-003

## 수행 순서 (권장)

1. Google Cloud Console에서 새 프로젝트 생성
2. OAuth 2.0 동의 화면 설정
3. 웹 앱 OAuth 클라이언트 생성
4. "승인된 리디렉션 URI"에 `https://zkcwbczpzkycpwhhrizk.supabase.co/auth/v1/callback` 추가
5. 클라이언트 ID/시크릿 복사
6. Supabase Dashboard > Authentication > Providers > Google에 입력

## done_when 확인 방법

- Google OAuth 클라이언트가 생성되었는가?
- Supabase에서 Google 인증이 활성화되었는가?
- 앱에서 "Google로 로그인" 버튼을 클릭했을 때 Google 로그인 페이지로 이동하는가?

## 확인 질문

현재 status: **needs_info** — "외부 계정 접근이 필요해 제가 대신 못함" (사용자 본인이 Google Cloud Console에서 설정해야 함)
