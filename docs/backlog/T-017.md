# T-017: 회원가입 시 profile 레코드 자동 생성 처리

> 문서 기준 backlog 스냅샷: sourceHash a816a52d678ed6d3…, 이 태스크의 현재 status: **todo**

## 목적

T-016의 로그인 UI 완성 후, 새 사용자가 소셜 로그인으로 가입할 때 `profiles` 테이블에 자동으로 레코드가 생성되도록 처리합니다.

## 쉬운 설명

사용자가 Google/Kakao/Naver로 처음 로그인하면, auth.users 테이블에 사용자 정보가 자동으로 생성되고, 이때 트리거가 동작하여 `profiles` 테이블에도 자동으로 기본 프로필이 생성됩니다(T-005에서 이미 트리거 설정됨).

## 필요한 입력

- T-005 (profiles 테이블 + 트리거)
- T-016 (로그인 UI)

## 결과물

- Supabase `handle_new_user` 트리거 검증/활성화 (이미 T-005에서 만들었을 수 있음)
- auth.users에 새 사용자 추가 시 profiles 레코드 자동 생성 확인

## 선행 작업

- T-005, T-016

## 수행 순서 (권장)

1. T-005에서 생성한 `handle_new_user` 트리거가 정상 동작하는지 확인
2. 로그인 후 Supabase 대시보드에서 profiles 테이블 확인
3. 새 사용자 레코드가 자동으로 생성되는지 테스트

## done_when 확인 방법

- 소셜 로그인 후 profiles 테이블에 자동으로 레코드가 생성되는가?
- 현재 로그인 UI 미구현 상태라 브라우저 E2E는 불가 — 코드 리뷰로 검증 필요

## 확인 질문

로그인 UI(T-016)가 미구현 상태이므로, 이 태스크도 실제 E2E 테스트는 T-016 완료 후 가능합니다.
