# T-007: Supabase DB 스키마: workspace(항목) 테이블 생성

> 문서 기준 backlog 스냅샷: sourceHash a816a52d678ed6d3…, 이 태스크의 현재 status: **done**

## 목적

개발요청서.md의 B. 데이터 모델 "항목(워크플레이스) 구조: 추천 카테고리 제공 + 사용자가 직접 새 카테고리 추가 가능"에 따라, 결혼 준비 항목(예: 결혼식장, 스튜디오)을 저장하는 `workspaces` 테이블을 생성합니다.

## 쉬운 설명

커플이 관리하는 각 항목(결혼식장, 메이크업 등)을 나타내며, 제목과 카테고리(추천 또는 직접 입력)를 저장합니다.

## 필요한 입력

- T-003 (Supabase 프로젝트)
- Supabase 마이그레이션 파일

## 결과물

- `workspaces` 테이블:
  - `id`, `couple_id`, `title`, `category` (추천 또는 custom)
  - `created_at`, `updated_at`
- RLS 정책 (couple 멤버만 접근)

## 선행 작업

- T-003 (Supabase 프로젝트)

## 수행 순서 (권장)

1. `npx supabase migration new create_workspaces_table`
2. workspaces 테이블 정의 (couple_id 참조)
3. RLS 정책: couple_members 중 하나여야 접근 가능
4. `npx supabase db push`

## done_when 확인 방법

- `workspaces` 테이블이 생성되었는가?
- RLS가 정상 작동하는가?
- 현재 상태: "workspaces 테이블+RLS 마이그레이션 적용 완료"

## 확인 질문

없음.
