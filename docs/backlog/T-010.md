# T-010: Supabase DB 스키마: budget 테이블 생성

> 문서 기준 backlog 스냅샷: sourceHash a816a52d678ed6d3…, 이 태스크의 현재 status: **done**

## 목적

개발요청서.md의 G. 기능 범위 "MVP 기능 범위: 예산 관리"에 따라, 결혼 준비 예산을 카테고리별로 관리하는 `budget_items` 테이블을 생성합니다.

## 쉬운 설명

커플이 함께 관리하는 예산 항목(카테고리별 예산 및 지출액)을 저장하여, 결혼식 준비 비용을 추적합니다.

## 필요한 입력

- T-006 (couples 테이블)

## 결과물

- `budget_items` 테이블:
  - `id`, `couple_id`
  - `category` (예: 결혼식장, 웨딩드레스)
  - `budgeted_amount`, `spent_amount` (예산액, 지출액)
  - `created_at`, `updated_at`
- RLS 정책

## 선행 작업

- T-006

## 수행 순서 (권장)

1. `npx supabase migration new create_budget_items_table`
2. couple_id, category, 금액 필드 정의
3. `npx supabase db push`

## done_when 확인 방법

- `budget_items` 테이블이 생성되었는가?
- 현재 상태: "budget_items 테이블+RLS 마이그레이션 적용 완료"

## 확인 질문

없음.
