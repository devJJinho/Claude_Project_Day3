{
  "name": "wedding-prep-site",
  "private": true,
  "type": "module",
  "description": "결혼 준비 사이트 — 애플리케이션 코드는 아직 없음(기술 스택 미정). 현재는 backlog 관리 CLI/훅/대시보드 도구만 있음.",
  "scripts": {
    "lint": "oxlint --deny-warnings .claude/hooks .claude/tools dashboard",
    "build": "node .claude/tools/check-build.mjs",
    "check:length": "node .claude/tools/check-code-length.mjs",
    "verify": "npm run check:length && npm run lint && npm run build"
  },
  "devDependencies": {
    "oxlint": "^1.83.0"
  }
}
